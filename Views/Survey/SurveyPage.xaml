<?xml version="1.0" encoding="UTF-8" ?>
<views:BasePage  
    x:Class="SpiritualGiftsSurvey.Views.Survey.SurveyPage"  
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"  
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"  
    xmlns:models="clr-namespace:SpiritualGiftsSurvey.Views.Controls"
    xmlns:views="clr-namespace:SpiritualGiftsSurvey.Views.Shared;assembly=SpiritualGiftsSurvey"  
    xmlns:vm="clr-namespace:SpiritualGiftsSurvey.Views.Survey;assembly=SpiritualGiftsSurvey"  
    xmlns:c="clr-namespace:SpiritualGiftsSurvey.Views.Controls;assembly=SpiritualGiftsSurvey"
    xmlns:ios="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls"
    ios:Page.UseSafeArea="false"
    ios:NavigationPage.HideNavigationBarSeparator="true"  
    x:DataType="vm:SurveyViewModel"
    NavigationPage.HasBackButton="False" 
    NavigationPage.HasNavigationBar="False"
    Shell.NavBarIsVisible="False"
    FlowDirection="{Binding FlowDirection}"
    BackgroundColor="{StaticResource ContentBackground}"
>

    <ContentPage.Content>
        <Grid RowDefinitions="Auto,Auto,*">
            <!-- BG -->
            <Image Source="mainpagebackground.jpg"
                Aspect="Fill"
                HorizontalOptions="Fill"
                VerticalOptions="Fill"
                Grid.RowSpan="3"
                IsVisible="{OnPlatform Android=true, iOS=false}" />

            <!-- NAVBAR -->
            <c:NavBar 
                Grid.Row="0" 
                Title="{Binding PageTopic}"
                ShowLeftButton="false"
                ShowRightButton="false"
                BackgroundColor="{StaticResource ContentBackground}" />

            <!-- NAVIGATION BAR -->
            <Grid 
                Grid.Row="1" 
                ColumnDefinitions="Auto,*,Auto"
                Padding="{OnIdiom Phone='20,10', Tablet='40,20'}"
                BackgroundColor="{StaticResource ContentBackground}">
                
                <!-- Previous Button -->
                <Button 
                    Grid.Column="0"
                    Text="&lt; Previous"
                    Command="{Binding PreviousPageCommand}"
                    IsEnabled="{Binding CanGoPrevious}"
                    BackgroundColor="{StaticResource AzureBlue}"
                    TextColor="White"
                    HeightRequest="{OnIdiom Phone=40, Tablet=60}"
                    WidthRequest="{OnIdiom Phone=120, Tablet=180}"
                    FontSize="{OnIdiom Phone=16, Tablet=24}"
                    CornerRadius="{OnIdiom Phone=20, Tablet=30}"
                    Margin="5,0" />

                <!-- Page Indicator -->
                <Label 
                    Grid.Column="1"
                    Text="{Binding PageIndicator}"
                    HorizontalOptions="Center"
                    VerticalOptions="Center"
                    FontSize="{OnIdiom Phone=18, Tablet=28}"
                    FontAttributes="Bold"
                    TextColor="{StaticResource TextPrimary}" />

                <!-- Next Button -->
                <Button 
                    Grid.Column="2"
                    Text="Next &gt;"
                    Command="{Binding NextPageCommand}"
                    IsEnabled="{Binding CanGoNext}"
                    BackgroundColor="{StaticResource AzureBlue}"
                    TextColor="White"
                    HeightRequest="{OnIdiom Phone=40, Tablet=60}"
                    WidthRequest="{OnIdiom Phone=120, Tablet=180}"
                    FontSize="{OnIdiom Phone=16, Tablet=24}"
                    CornerRadius="{OnIdiom Phone=20, Tablet=30}"
                    Margin="5,0" />
            </Grid>

            <Grid Grid.Row="2">
                <BoxView BackgroundColor="{StaticResource ContentBackground}" />

                <CollectionView
                    x:Name="QuestionsCollectionView"
                    ItemsSource="{Binding CurrentPageQuestions}"
                    VerticalOptions="Fill"
                    HorizontalOptions="Fill">

                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="models:QuestionViewModel">
                            <c:QuestionView Question="{Binding .}">
                                <c:QuestionView.Margin>
                                    <MultiBinding Converter="{StaticResource ItemMarginConverter}">
                                        <Binding Path="IsFirst" />
                                        <Binding Path="IsLast" />
                                    </MultiBinding>
                                </c:QuestionView.Margin>
                            </c:QuestionView>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

                <!-- Top fade -->
                <BoxView
                    VerticalOptions="Start"
                    HeightRequest="{OnIdiom Phone=50, Tablet=100}"
                    InputTransparent="True"
                    IsVisible="{OnPlatform Android=true, iOS=false}">
                    <BoxView.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                            <GradientStop Color="{StaticResource ContentBackground}" Offset="0" />
                            <GradientStop Color="Transparent" Offset="1" />
                        </LinearGradientBrush>
                    </BoxView.Background>
                </BoxView>

                <!-- Bottom fade -->
                <BoxView
                    VerticalOptions="End"
                    HeightRequest="{OnIdiom Phone=50, Tablet=100}"
                    InputTransparent="True"
                    IsVisible="{OnPlatform Android=true, iOS=false}">
                    <BoxView.Background>
                        <LinearGradientBrush StartPoint="0,1" EndPoint="0,0">
                            <GradientStop Color="{StaticResource ContentBackground}" Offset="0" />
                            <GradientStop Color="Transparent" Offset="1" />
                        </LinearGradientBrush>
                    </BoxView.Background>
                </BoxView>

                <!-- Confirm button pinned at bottom on top of content -->
                <Button
                    Command="{Binding FinishCommand}"
                    HorizontalOptions="Fill"
                    VerticalOptions="End"
                    BackgroundColor="{StaticResource EmeraldGreen}"
                    IsVisible="{Binding ShowConfirmButton}"
                    Text="{Binding ContinueText}"
                    TextColor="White"
                    Margin="{OnIdiom Phone='80,40', Tablet='160,80'}"
                    HeightRequest="{OnIdiom Phone=50, Tablet=80}"
                    FontSize="{OnIdiom Phone=30, Tablet=40}"
                    Padding="0"
                    CornerRadius="{OnIdiom Phone=25, Tablet=40}" />
            </Grid>
            
            <ContentView x:Name="ActIndBackground" BackgroundColor="#222222" Opacity="0.65" Grid.RowSpan="3" IsVisible="{Binding IsLoading}" />
            <StackLayout Orientation="Vertical" IsVisible="{Binding IsLoading}" WidthRequest="300" HeightRequest="100" HorizontalOptions="Center" VerticalOptions="Center">
                <ActivityIndicator IsRunning="True" Color="{StaticResource WhiteSmoke}" />
                <Label Text="{Binding LoadingText}" HorizontalOptions="Center" TextColor="{StaticResource WhiteSmoke}" />
            </StackLayout>
            
        </Grid>
    </ContentPage.Content>
</views:BasePage>
